
# use msmtp or opensmptd sudo pacman install after sudo systemctl enable cronie && start cronie

*/40 * * * * /bin/bash -l -c  rsync -azP euler@192.168.1.169:/home/euler/.config/zsh ~/raspberry-dotfiles


1 22 * * * /bin/bash -l -c cron-ten-thirty
0 */3 * * * /bin/bash -l -c cron-every-two-hours



#Example with opensmtpd
#Install opensmtpd.

#Edit /etc/smtpd/smtpd.conf. The following configuration allows for local delivery:

#listen on localhost
#action "local" mbox alias <aliases>
#3match for local action "local"
#You can proceed to test it. First start smtpd.service. Then do:

#$ echo test | sendmail user
#user can check their mail i
